# Core dependencies
fastapi
uvicorn[standard]
sqlalchemy
psycopg
python-dotenv
pydantic
pydantic-settings
email-validator
pytz

# Authentication
python-jose[cryptography]
bcrypt

# Optional: Firebase (install separately if needed)
# firebase-admin
